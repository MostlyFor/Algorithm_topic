#include <string>
#include <vector>
#include <algorithm>

using namespace std;

vector<string> solution(vector<string> orders, vector<int> course) {
    vector<string> answer;
    // 단품 요리 -> 코스 요리
    // 코스 길이는 나와있는대로
    
    // orders 크기가 20 이하면 완탐해도 될 것 같
    // 
    return answer;
}