백준 분할정복 문제입니다.


1. 백준 1780번 - 종이의 개수
    - a. c++은 여러값 return이 안되므로 참조를 이용하여 답을 내기 위해 답에 필요한 변수 설정
    - b. 2차원 vector로 map 받기 O(K^2)
    - c. k를 점차 줄이면서 크기가 k^2인 종이 체크하기 O(3*M* K^2) <- 전체 맵 크기 K^2, 3^M = K, (-1,0,1) =3가지
    (이건 재귀함수로 구현)

    어색했던 점:
        2차원 분할탐색이 좀 어색했음. while문을 가장 바깥으로 2개 빼야함.